pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        repositories {
            mavenCentral()
            maven { url 'https://jitpack.io' }
        }
    }
}
rootProject.name = "CardWatcherApp"
include ':app'
